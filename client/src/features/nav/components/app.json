{
  "healthchecks": {
    "web": [
        {
            "type":        "startup",
            "name":        "web check",
            "description": "Checking if the app responds to the /api/queue endpoint",
            "path":        "/api/queue",
            "attempts": 5
        }
    ]
  }
}
